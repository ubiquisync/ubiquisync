[package]
name = "ubiquisync-tables"
description = "Table protocol for Ubiquisync — structured rows synced over commodity cloud storage or a dedicated server."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
# Exposes `test_support`: a backend-agnostic physical-schema suite the driver
# crates run against their real `Db` (see `ubiquisync-sqlite`'s dev-dependencies).
test-support = []

[dependencies]
ubiquisync-core = { workspace = true }
ubiquisync-sql = { workspace = true }
bitfield-struct = { workspace = true }
thiserror = { workspace = true }
async-trait = { workspace = true }
futures = { workspace = true }
pastey = { workspace = true }
sea-query = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
test-strategy = { workspace = true }

[lints]
workspace = true
